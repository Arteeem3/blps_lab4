<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- Disable all ServletContainerInitializers except Spring's. 
         This prevents Jersey's auto-discovery from initializing Camunda's
         ResourceConfig outside of the Spring context, which causes the 
         ModelValidationException because it misses our Spring-configured 
         RESOURCE_VALIDATION_DISABLE property. -->
    <absolute-ordering>
        <name>spring_web</name>
    </absolute-ordering>

</web-app>
